.env
__pycache__/
debug_output*.txt
pipeline_log.txt
*.pdf
scratch/

# Node
node_modules/
dist/
dist-ssr/
*.local

# Vite
.vite/

# Database
jobagent.sqlite
job_hunter.db
jobagent_temp.sqlite
jobagent.sqlite-shm
jobagent.sqlite-wal

# OS and IDE
.DS_Store
.idea/
.vscode/

# Build artifacts
*.zip
*.log

# Browser and Cache
data/browser_context/
.tempmediaStorage/

# External dependencies (local only)
OpenPostings-extracted/
OpenPostings-main.zip
career-ops-main/

# Pipeline runtime artifacts (generated by scout/pipeline runs) — root only; do not use bare jobs/ (ignores server/routes/jobs/)
/jobs/
submissions/
/archive/

# Materialized from SQLite — source of truth is the Job Search settings UI
data/candidate_preferences.json

# Local-only docs (not for public repo)
docs/PROJECT_DEEP_DIVE.md

# Claude Code internal files
.claude/
settings.local.json
*.sqlite*

# User/Auditor custom ignores
tmp_audit.txt
audit_report.md
data/scouted_jobs_log.txt
*.csv

# Data Privacy & Wildcard Protection
# Ignore all real data files to prevent leaking personal career info to public repos
data/*.md
data/*.json

# Explicitly ALLOW the example templates to be tracked
!data/*.example.md
!data/*.example.json
