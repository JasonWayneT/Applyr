---
status: not_started
created: 2026-07-17
related: CR-059 (local-llm-tuning-loop, closed_moot — this CR corrects a scoping gap in its finding), CR-053/054/055 (fit-rubric overhaul, owns the scoring formula this CR relocates but does not change), CR-062 (local-rewrite-harness, untouched), CR-064 (claim-score-formula-rework, untouched)
contains: CR-070 (Claude-Native Generation Pipeline)
---

# CR-070 — Claude-Native Generation Pipeline: Epics & Stories

> **Superseded for generation (2026-07-19)** — see the same note at the top of the CR-070 spec. Epics 2 and 3 survived the 2026-07-18 direct-authoring pivot; the rest (built around reusing `draft_compiler.py`'s deterministic assembly path) did not. Current process: `.claude/skills/generate-submission/SKILL.md`.

**Handoff doc, one change request.** Resumable plan for rearchitecting the resume/cover-letter
generation pipeline to run inside Claude Code on the subscription (no API billing), batch-capable
across 24+ JDs with minimal permission-prompt friction, without touching the deterministic
grounding/rubric/voice machinery that already works. A new session can pick up at the first unchecked
story with no other context needed beyond this file, [CR-070 spec](../05-change-requests/CR-070-claude-native-generation-pipeline.md),
and the Phase 1 investigation findings summarized in that spec's Problem section.

## Why this exists (do not re-litigate without re-reading this)

Phase 1 investigation (2026-07-17 session) found CLAUDE.md's inherited claim from CR-059's closure
("the default drafting pipeline calls zero local LLMs") is stale. CR-059's own tracker doc actually
already scoped fit-scoring **out** of its "zero LLMs" finding by name ("Redirecting to the LLM calls
that do run live (fit scoring, research packets, interview cheat sheets) would cross this CR's own
guardrail") — the finding was correct and narrow, but CLAUDE.md's summary of it lost that nuance and
now reads as an unqualified claim. Two call sites are live LLM calls on the default path today:

1. `evaluate_job_fit` → local Ollama, up to 5x per job, up to 180s timeout each (`scripts/batch_pipeline.py:813`, `scripts/llm_stages.py:57`)
2. `audit_and_improve.audit_and_improve_company`, called unconditionally post-draft (`scripts/drafting_engine.py:391`), up to 8 more LLM round trips rewriting already-deterministic output — this one was not previously named by any CR in the registry.

These two, plus 4-10+ Playwright re-renders per job, are the concrete, evidence-backed contributors to
the 5-10 minute per-JD time. No manual-approval prompts exist anywhere in the generation code itself —
if approval-click friction is being felt, it's coming from the Claude-Code-harness layer (Bash
permission prompts on repeated script invocations across a batch), not the Python pipeline. Epic 5
addresses that directly.

## Open decisions — resolved 2026-07-17

- [x] **Is `audit_and_improve.py`'s rewrite loop known to have ever caught a real defect the rubric gate wouldn't?** RESOLVED — investigated directly (code read + `git log --follow`, see spec doc). Answer: no, it's not redundant motion. It does three real things the deterministic draft and the rubric gate don't: company-stage-aware tailoring of summary/cover letter (not just keyword overlap), a second independent numeric/fact grounding check with a retry-feedback loop, and a snapshot/restore safety net so failed runs never leave broken output. Epic 3 below now ports this logic to Claude-native reasoning rather than deleting it, running as a distinct step *before* the rubric gate, not a replacement for it. Original note kept below for record: Epic 3 deletes it on the assumption it's redundant motion, not a safety net. Before deleting, Epic 3's first story should search for any evidence (commit messages, past submission diffs) it fixed something real. If it did, that behavior needs to be preserved inside the new rubric-gate pass, not just dropped.
- [x] **Losing Ollama as a second, independent model for fit evaluation** — RESOLVED, Jason's decision: use the Claude-native reasoner. Single coherent reasoner, no Ollama dependency on the default path. Epic 2 implements this; the old path is kept in the repo (not deleted) behind an opt-in flag per the "keep code intact" decision below. Original note kept below for record: — today `evaluate_job_fit` and Claude's own drafting judgment are two separate reasoners. Moving fit evaluation to Claude-native reasoning makes it one coherent, more auditable reasoner, but removes that independence. Is that a net improvement or a regression worth keeping the Ollama path for, opt-in? Default recommendation: single coherent reasoner (simpler, faster, no infra dependency) — flag for explicit approval before Epic 2 ships.
- [x] **Keep application code intact for research/study** — Jason's explicit instruction (2026-07-17), applies repo-wide to this CR: nothing gets deleted. `audit_and_improve.py` and the Ollama fit-call path stay on disk, unmodified, reachable via an explicit opt-in flag (default off). Every story below that previously said "remove"/"retire" now says "gate off by default" instead.

## Epic 1 — Fix the stale documentation claim

Quick, standalone, no code risk. Do this first regardless of what happens with the rest of the CR.

- [ ] **Story 1.1**: Correct CLAUDE.md's CR-059 reference to name fit-evaluation and `audit_and_improve` as live LLM call sites (not "zero local LLMs" unqualified). Mirror the exact same edit into AGENTS.md per the repo's byte-identical rule.
- [ ] **Story 1.2**: Update CR-059's tracker doc status/related-CRs to reference this CR-070 as the follow-up that acted on the fit-scoring/audit-and-improve call sites it had explicitly scoped out.

## Epic 2 — Fit evaluation: Ollama subprocess → Claude-native reasoning

**Scope corrected 2026-07-17 (superpowers plan Story 2.1 trace).** The original Epic 2 wording targeted `_call_fit_llm`/`_call_fit_scoring_only` (`scripts/batch_pipeline.py:661-946`) — CR-035's legacy holistic 0-100 scorer, up to 5 Ollama calls at up to 180s timeout each. Traced the real live call graph before planning further: `evaluate_job_fit` (`batch_pipeline.py:813`) checks `structured_fit_enabled()` (`structured_fit.py:85-87`, defaults **on** — `STRUCTURED_FIT` env var default `"1"`) and, when true, calls `evaluate_structured_fit` (`structured_fit.py:400-416`, CR-053) and **returns immediately on success** (`batch_pipeline.py:881`) — the entire legacy multi-retry ladder below it (`batch_pipeline.py:883-940`) is dead code on the default path today, only reached if structured scoring is disabled or fails outright.

The real default-path LLM call is `_call_equivalence_llm` (`structured_fit.py:218-281`), invoked once per JD from inside `evaluate_structured_fit` (`structured_fit.py:411-412`). It's already narrowly scoped — yes/partial/no judgments per must-have plus 5 fixed criteria, explicit system instruction "Never output a fit score number" — with `_heuristic_judgments` (`structured_fit.py:191-215`, zero-LLM keyword overlap) as an already-built fallback when the call fails, and all actual scoring math (`compute_fit_report`, `structured_fit.py:306-397`) is deterministic Python, not LLM. This is a much better-designed system than the original Epic 2 wording credited — CR-053 already solved most of what Epic 2 assumed was still broken. Net effect: Epic 2 removes **one** Ollama round trip per job, not five. Correcting this now rather than building against a premise that doesn't match current code.

- [x] **Story 2.1**: RESOLVED — traced above. Real target: `_call_equivalence_llm` (`structured_fit.py:218-281`), input = `(jd_text, work_exp, must_haves)`, output schema = `{"must_haves": [{"text","judgment","justification"}...], "criteria": {<5 fixed keys>: {"judgment","justification"}}}`, consumed by `_normalize_judgments` (`structured_fit.py:284-303`) then `compute_fit_report` (deterministic, unchanged).
- [ ] **Story 2.2**: Build the skill step where Claude reads the JD text + must-haves (from `extract_must_haves`, already deterministic, unchanged) + candidate profile excerpt and produces the same `{"must_haves": [...], "criteria": {...}}` JSON shape directly (Write tool), replacing the `call_llm_stage("fit_equiv", ...)` call in `_call_equivalence_llm`. `compute_fit_report`, `_normalize_judgments`, `_heuristic_judgments`, and all of `fit_policy.py`'s downstream gates stay untouched — only the judgment source changes.
- [x] **Story 2.3**: DONE 2026-07-17 — ran a real 10-JD comparison (`docs/superpowers/plans/2026-07-17-cr070-epic1-epic2.md` Task 4). Method: picked 10 archived JDs (`data/archive/submissions/`) that also have a stored historical `jobs.score` in `jobagent.sqlite` (snapsheet, roadie, quinstreet, pointclickcare, tivity_health, apex_systems, ryan, swarm_aero, rafay, informdata). Ollama is not reachable in this environment (confirmed — `localhost:11434` connection refused), so a live same-session Ollama-vs-Claude-native head-to-head wasn't possible; compared Claude-native judgments (reasoned directly against each real JD + `data/workExperience.md`, written via `fit_judgment_io.write_equivalence_judgment`, run through the real `evaluate_structured_fit(FIT_JUDGMENT_MODE=claude_native)`) against each job's stored historical `jobs.score` instead.

  **Caveat that matters:** the historical scores were not necessarily produced by today's exact `compute_fit_report` code — fit/claim scoring changed multiple times across CR-053/064/065/066/067/068 since those jobs were originally scored, so this is not a clean same-algorithm before/after. It's the best available real-world baseline without a reachable Ollama endpoint, not a controlled experiment.

  | Company | Historical | Claude-native | Decision (72 threshold) | Diff |
  |---|---:|---:|---|---:|
  | snapsheet | 85 | 96 | YES | +11 |
  | roadie | 97 | 100 | YES | +3 |
  | quinstreet | 85 | 68 | **NO** | -17 |
  | pointclickcare | 75 | 90 | **NO** (hard-fail on healthcare must-have) | +15 |
  | tivity_health | 60 | 96 | YES | +36 |
  | apex_systems | 96 | 90 | YES | -6 |
  | ryan | 100 | 88 | YES | -12 |
  | swarm_aero | 78 | 62 | **NO** | -16 |
  | rafay | 85 | 90 | YES | +5 |
  | informdata | 85 | 96 | YES | +11 |

  Mean absolute difference: 13.2 points. **8/10 agree on Decision direction at the min_fit_score=72 threshold; 2/10 disagree** (quinstreet, swarm_aero — both cases where the Claude-native judgment flagged a real domain/craft mismatch: quinstreet is consumer-fintech growth/experimentation work, not Jason's documented platform-stabilization craft; swarm_aero is defense/UAV real-time systems, explicitly stated as not a role "for someone learning the craft"). pointclickcare is a third notable case: Claude-native correctly hard-failed on the healthcare-background must-have even though the weighted score alone (90) would have passed — Jason has zero healthcare/clinical domain experience, and CR-053's own founding diagnosis specifically named domain-mismatch as a failure mode the old scoring missed.

  **Read on this, not just the numbers:** the two decision flips aren't random noise — both are explainable by genuine domain/craft-fit reasoning that plausibly predates CR-053's domain-awareness work. But 10 JDs and one reasoner's pass is not enough to declare this settled either way. **Recommendation: do not flip `FIT_JUDGMENT_MODE` to `claude_native` by default yet.** Widen the sample (aim for 30-50, spanning more of the archive) and, critically, get Jason's read specifically on the quinstreet and swarm_aero calls — do those two flips match his own judgment of those two roles, or does the Claude-native reasoning have a real, correctable bias? That answer should gate the default flip, not an aggregate diff number alone.

  **Widened to 34 JDs, 2026-07-17.** Per the above, ran 24 more archived JDs (4 parallel judgment passes, 6 companies each, same method) to check whether the 20% disagreement rate on the first 10 held up or was noise.

  | Batch | Companies | Decision agreement | Notable disagreements |
  |---|---|---|---|
  | Original 10 | snapsheet, roadie, quinstreet, pointclickcare, tivity_health, apex_systems, ryan, swarm_aero, rafay, informdata | 8/10 | quinstreet (fintech/growth craft), swarm_aero (defense/UAV domain) |
  | Batch 1 | OpenRouter, Enlyte, Tekion, Tillster, Covideo, Clerkie | 2/6 | OpenRouter (enterprise sales-GTM), Enlyte (billing/revenue — explicit exclusion zone), Tekion + Covideo (automotive, explicit required line) |
  | Batch 2 | PracticeTek, Veeva Systems, Reveille Technologies, Forbes, Mandolin, Vendelux | 4/6 | Reveille (payments must-have), Mandolin (AI-agent + early-stage — **two** explicit exclusion zones at once) |
  | Batch 3 | IBBX, Amplify, Experian, Affinity.co, Solenis, Koalafi | 4/6 | Solenis (not a software PM role at all — industrial chemical P&L/regulatory, title-keyword false positive), Koalafi (below-threshold historical vs. strong craft match found here) |
  | Batch 4 | Ltk Usa, Revalize, GE Healthcare, Private Health Management, Crewfare, Innovative IT Solutions | 5/6 | GE Healthcare (clinical/regulated domain, no team overlap with verified partner list) |
  | **Total (34)** | | **23/34 (68%)** | 11 disagreements, all in the same direction (historical score higher than Claude-native) |

  **This did not converge toward agreement — the disagreement rate on the wider sample (11/34, 32%) is higher than the first 10 (20%), and every single disagreement resolves the same direction: the historical score was inflated relative to a real, explainable domain/craft gap.** Solenis is the starkest case — the historical pipeline applied to a Regional Product Manager role that isn't a software PM job at all (industrial water/chemical SBU ownership, P&L, regulatory strategy, chemistry/engineering degree required), which reads like a pure title-keyword false positive ("Product Manager") rather than any real craft evaluation. This pattern — inflated historical scores on explicit-but-unheeded domain requirements (automotive, billing/payments, life sciences, AI-agent/early-stage, industrial manufacturing, clinical/healthcare) — lines up exactly with CR-053's own founding diagnosis that domain mismatch was excluded from scoring by prior policy (CR-039) with no compensating signal.

  **Important caveat this finding does NOT resolve:** these historical `jobs.score` values may predate CR-053 entirely, or predate whatever specific scoring-formula iteration was live when each job was processed (CR-053/064/065/066/067/068 all changed scoring across several weeks). This comparison cannot distinguish "Claude-native judgment beats the *current* Ollama-based `_call_equivalence_llm`" from "Claude-native judgment beats an *older, pre-domain-awareness* scoring regime that current Ollama-based scoring may have already fixed." Ollama isn't reachable in this environment, so a live current-Ollama-vs-Claude-native head-to-head remains untested.

  **Revised recommendation:** the judgment quality itself looks trustworthy and consistent, not the blocker anymore. The real blocker to flipping `FIT_JUDGMENT_MODE` by default is that **no automated mechanism exists yet to populate `fit_judgment.json` during a real batch run** — every result in this validation was produced by a human-in-the-loop-style reasoning pass (an agent manually reading each JD and writing the file), not something that happens automatically when the pipeline processes a new JD end-to-end. Flipping the default today would just make every job fall through to `_heuristic_judgments` (no file present), which is a regression, not an upgrade. **The actual next step is Epic 5** — build the real skill/orchestration that performs this judgment inline during a live pipeline run — not a config flip. Once that exists, re-run this same 34-JD (or larger) comparison through the *automated* path before deciding on the default.
- [x] **Story 2.4**: RESOLVED per "keep code intact" decision — gate `_call_equivalence_llm`'s Ollama call behind an explicit opt-in flag (e.g. `FIT_JUDGMENT_MODE=ollama_legacy`, default off) once 2.3 passes. Leave `_call_equivalence_llm` and the dead legacy ladder (`_call_fit_llm`/`_call_fit_scoring_only`) callable, just not on the default path.

## Epic 3 — Port `audit_and_improve.py`'s tailoring + safety-net logic to Claude-native reasoning

Investigated 2026-07-17 (see resolved Open Decision above): `audit_and_improve.py` does real work — company-stage-aware tailoring, a second independent grounding check, and safe rollback on failure — not redundant motion. This epic ports its three `call_llm` functions to Claude-native reasoning and reuses its deterministic guard logic as-is; the file itself is not deleted or modified, only stops being called by default. This step runs *before* the rubric gate (Epic 3), not instead of it — the rubric gate is a separate, later check (folded in as part of Epic 5's skill assembly).

- [x] **Story 3.1**: Investigate whether `audit_and_improve.py`'s rewrite loop has ever fixed a real defect. **Done — see resolved Open Decision above.** Finding: yes, real value in three places — stage-aware tailoring, independent numeric/fact re-check with retry-feedback, snapshot/restore safety net.
- [ ] **Story 3.2**: Build the Claude-native replacement for `analyze_company_context` — Claude classifies company stage/motion/segment/pain points/partners from the JD inline during the skill turn, no `call_llm` subprocess.
- [ ] **Story 3.3**: Build the Claude-native replacement for `improve_resume_summary` and `improve_cover_letter` — Claude rewrites the summary/cover letter for stage-fit using the same factual constraints already encoded in the current prompts (grounded-only metrics, no em-dashes, exactly 3 summary sentences), writing directly via the Edit tool. Do not carry forward `improve_cover_letter`'s hardcoded example closer (see spec doc's implementation note) — let Claude generate a fresh closer per letter.
- [x] **Story 3.4**: Reuse the existing deterministic guard functions unchanged — `validate_hard_facts`, `audit_text_against_bullet_corpus`, the 3-sentence-count regex check, and the snapshot/restore pair (`_snapshot_submission_assets`/`_restore_submission_assets`) — calling them against Claude's edits instead of the old LLM output. Preserve the retry-with-feedback loop shape (up to 3 attempts, specific error fed back on retry). **Done 2026-07-17** — see validation pass and fix below.

  **Validation pass, 2026-07-17** (`docs/superpowers/plans/2026-07-17-cr070-epic3.md` Task 2; box left unchecked, see verdict below). Method: picked 5 real archived companies with both `Resume.md`/`CoverLetter.md` (`data/archive/submissions/{snapsheet,roadie,rafay,informdata,tillster}`), extracted each one's real `PROFESSIONAL EXPERIENCE` bullets, hand-authored a genuinely improved, company-stage-aware 3-sentence summary and cover-letter body per company grounded only in those bullets plus `data/workExperience.md`, spliced each into a full document (real header/experience/education kept as-is), and called the real `apply_claude_native_improvement(updated_resume, updated_cl, master_resume_text, bullets, company_name)` from `scripts/audit_improve_native.py` — no mocking. For 2 of the 5 (roadie, tillster) also ran a deliberately-fabricated variant (an invented percentage not in any bullet or in `workExperience.md`) to confirm the guard rejects it for the right reason.

  | Company | Mode | Converged | Root cause when False |
  |---|---|---|---|
  | snapsheet | good-faith | **False** | EDUCATION regex bug only (below) |
  | roadie | good-faith | **False** | EDUCATION regex bug only |
  | rafay | good-faith | **False** | EDUCATION regex bug only |
  | informdata | good-faith | **False** | EDUCATION regex bug only |
  | tillster | good-faith | **False** | EDUCATION regex bug + Pendo tool-blocklist bug |
  | roadie | fabricated (+35% revenue) | **False** (correctly) | `Summary metric error: ... numbers not in bullet corpus: ['35']` — exact fabricated number named, plus the EDUCATION bug |
  | tillster | fabricated (+65% churn cut) | **False** (correctly) | `Summary metric error: ... numbers not in bullet corpus: ['65']` — exact fabricated number named, plus the EDUCATION bug + Pendo bug |

  **The two fabrication cases are a clean pass**: `audit_text_against_bullet_corpus` (the actual new logic Story 3.4 is about) named the exact invented number both times, no false negative and no wrong-reason rejection. Sentence-count and per-company bullet-corpus checks also passed cleanly on all 5 good-faith summaries (never appeared in `final_issues`). The problem is that **none of the 5 good-faith, non-fabricated rewrites converged** — but a follow-up correction pass (below, 2026-07-17, same session) found the validation subagent's initial bug list was itself partly wrong, and re-verified everything directly before trusting it.

  **Correction after independent re-verification.** The subagent's report named 5 "pre-existing bugs" in `validate_hard_facts`. Checked each directly rather than accepting the report at face value:

  1. **EDUCATION header regex — real bug, but its practical severity was overstated.** `header_mappings`'s EDUCATION tuple (`drafting_engine.py` ~line 141, `r'^#{2,3}\s*(?:\*\*)?EDUCATION(?:\s+.*)?(?:\*\*)?'` → `'## EDUCATION'`) does delete the degree line in isolation — confirmed by direct regex repro. **But `validate_hard_facts` has a later self-healing step that detects the resulting missing education fact and re-injects a corrected block before returning** (`[GUARD] Auto-injected verified Education block`) — confirmed by running the *full* function (not just the regex) on a realistic complete resume: the returned `corrected_resume` had the degree line intact (`EDUCATION section survived intact: True`), alongside a `"MISSING FACT: Education... Triggering self-healing injection"` warning. So this is not silent data loss in practice — it's a spurious/confusing warning about damage the function fixes itself. The real, actionable bug this exposes is in **Epic 3's own new code**, not `drafting_engine.py`: `apply_claude_native_improvement` treats every `validate_hard_facts` warning as a blocking `issues` entry, with no way to distinguish "real unresolved problem" from "warned but successfully self-healed." That's why every good-faith rewrite failed to converge. **Fix needed in `audit_improve_native.py`, not `drafting_engine.py`** — either trust `corrected_resume`/`corrected_cover_letter` when a warning indicates successful self-healing, or filter out self-healing-confirmation warnings from the blocking `issues` list.
  2. **"`master_resume_text` = `workExperience.md` breaks name detection" — this was a test-setup mistake, not a production bug.** The validation subagent (and my own initial spot-check) passed `data/workExperience.md` as `master_resume_text`. Checked what production actually passes: `RESUME_MASTER_FILE` (`scripts/utils.py:28`) points to `data/Resume.md`, not `workExperience.md` — and `data/Resume.md` starts with `# JASON TAYLOR` (confirmed directly), so the real name-detection check passes fine in the actual calling convention both the legacy path and this new module are meant to use. Struck from the bug list — this one doesn't block anything in real usage. Worth a note for Epic 3 Task 2 if re-run: pass `data/Resume.md`, not `workExperience.md`, as `master_resume_text`.
  3. **`HARD_FACTS` module-level-global caching** — plausible architectural fragility (first `master_resume_text` in a process wins for all later calls), not independently re-verified against the corrected file in this pass. Worth a real test before treating as confirmed, given finding 2 above shows the subagent's setup wasn't using the production file.
  4. **`BLOCKED_TOOLS` contains "Pendo," contradicting `workExperience.md` ACC-117's approved usage** — not independently re-verified in this correction pass; flagged as-is for someone to confirm before acting on it.
  5. **`apply_claude_native_improvement`'s unhandled `IndexError`** on resumes without a literal `## PROFESSIONAL SUMMARY` markdown heading (older HTML-templated archives use `<h2>` instead) — real and unchanged by this correction; low severity, only affects older archived formats, not the current required template.

  **Verdict: do not check Story 3.4 done, but the real blocker is smaller and entirely inside Epic 3's own new code, not `drafting_engine.py`.** No production data-loss bug exists — the scare in the first pass came from testing against the wrong ground-truth file plus not tracing `validate_hard_facts`'s self-healing far enough. The actual fix needed: make `apply_claude_native_improvement` distinguish self-healed warnings from real failures. Small, contained, doesn't touch reused legacy code at all.

  **Fix applied, 2026-07-17.** `audit_improve_native.py` now filters `validate_hard_facts`'s warning list before treating it as blocking, via a `_is_self_healed_warning` check against a marker allowlist (education re-injection, name/contact header repair, known-hallucination substitution, blocked-tool redaction, tone rewrite, em-dash replacement — every case in `drafting_engine.py` where the warning text describes damage the function has already fixed in the `corrected` text it returns). Warnings **not** on the allowlist (`SENIORITY INFLATION`, `METRIC INTEGRITY`, `MISSING FACT: Company` — none of which `validate_hard_facts` auto-repairs) still block, unchanged. Verified two ways: (1) a new regression test (`test_self_healed_education_warning_does_not_block_convergence`) reproduces the exact failure from the validation pass above — confirmed to fail against the pre-fix code (`converged=False` on the education self-heal warning) and pass after; (2) a new `test_seniority_inflation_warning_still_blocks` proves the filter didn't become overly permissive — a real, non-healed warning still returns `converged=False`. Full suite: `cd scripts && python -m pytest test_audit_improve_native.py -v` — 6/6 pass. Guard functions themselves (`validate_hard_facts`, `audit_text_against_bullet_corpus`) are unchanged; only `audit_improve_native.py`'s own warning classification was fixed. The 5-company good-faith re-validation from the pass above was not re-run against the fix (would require re-running Task 2's exact inputs), so treat this as unit-test-verified, not re-confirmed against the original 5 archived companies.
- [x] **Story 3.5**: RESOLVED per "keep code intact" — gate the unconditional call to `audit_and_improve_company` in `drafting_engine.py:391` behind an opt-in flag (default off) once 3.2-3.4 are validated on a sample batch; do not delete the function or the file.

## Epic 4 — Reduce redundant PDF renders

- [x] **Story 4.1**: Confirm the page-pruning loop (`draft_compiler.py:946-981`, capped at 8 iterations) is untouched and still correct in isolation.

  **Done 2026-07-18.** `draft_compiler.py` was not touched by any of this session's work (Epics 2/3/5, the Ollama-call fixes) — confirmed via the actual diff, not just recollection. Read `_pdf_page_prune()` (946-981) directly: generates the resume PDF once, and only if `get_pdf_page_count(resume_pdf) > 1` does it prune the lowest-JD-relevance bullet from the lowest-priority employer and regenerate, capped at 8 attempts, breaking early once no employer has bullets above its quota floor to prune further. Structurally correct and bounded — no changes needed. Cover letters have no equivalent PDF-prune loop; length is governed by a word-count budget check (250-400 words) instead, confirmed separately as the actual CL-side mechanism (matches the Story 5.3 dry run's own `Cover audit notes: Word count high` output) — not a gap, just a different, appropriate mechanism for a document that doesn't accumulate bullets.
- [x] **Story 4.2**: Make the post-gate re-render (spec step 9) conditional on step 7 (audit-and-improve port) or step 8 (rubric gate) actually having edited Resume.md/CoverLetter.md, not unconditional.

  **Done 2026-07-18.** Already written this way in `.claude/skills/generate-submission/SKILL.md`'s Step 9, and actually exercised for real in the Story 5.3 dry run (only re-rendered after confirming Step 7 had edited the files). Tightened today: the original wording gated re-render behind one shared `step7_edited_files or step8_edited_files` boolean, which would still over-render — e.g. re-rendering an unchanged CoverLetter.pdf just because Step 7 only touched Resume.md. Changed to track `resume_edited`/`cl_edited` independently, so each document only re-renders if it specifically changed.
- [x] **Story 4.3**: Measure real render count per job before/after Epics 2-4, confirm it drops from the observed 4-10+ to at most 2 (initial + conditional re-render).

  **Done 2026-07-18, with an honest scope correction — the "at most 2" target is real but only applies to the part of the flow this CR actually rearchitected.** Traced where `draft_compiler.py`'s `generate_pdf` calls actually live: the resume goes through `for attempt in range(1, max_attempts + 1)` (`conversion_retry_max()`, default 2, `pipeline_env.py:228-230`) — each attempt renders once (line 991) *and* can trigger up to 8 more prune-renders inside `_pdf_page_prune()` (line 974) if the page count is still over 1 — plus one cover-letter render (line 1104, outside the resume loop, not multiplied by attempts). **This retry+prune mechanism lives entirely inside steps 2/4/5/6, which CR-070's own spec explicitly reuses unmodified** — it's the real, dominant source of the historical "4-10+" figure, and it wasn't in scope to change here. The part CR-070 *did* rearchitect is the post-draft tail: the old `audit_and_improve.py` always attempted a rewrite and rendered exactly once more on convergence (checked directly — its `generate_pdf` calls, lines 295-296, sit after the loop's only `return`, not per-attempt, so it wasn't as wasteful per-attempt as first assumed) — but with zero conditionality, meaning it rendered even when the initial draft needed no changes at all. The new flow's Step 7-9 tail (Story 4.2) only renders when something actually changed. **Real measured data, not estimated:** the Story 5.3 dry run's actual observed count was 2 renders per document total (1 initial in Step 6, 1 conditional in Step 9 after Step 7's edit converged) — hitting the "at most 2" target for that run. That figure is JD-dependent, not a structural guarantee for every JD, since a harder JD needing multiple page-prune iterations would still show a higher count from the unmodified steps 2-6 mechanism, independent of anything this CR changed. No further code change made here — Epic 4's actual scope (the post-draft tail) is already correctly conditional per Story 4.2.

## Epic 5 — Package as a Skill; fix the manifest gap

- [x] **Story 5.1**: Write `.claude/skills/generate-submission/SKILL.md` encoding the 11-step flow from the CR-070 spec (including step 7's audit-and-improve port and step 8's rubric gate as sequential, distinct steps), callable per-JD.

  **Done 2026-07-17.** `.claude/skills/generate-submission/SKILL.md` written, all function/module names and signatures verified against the real files rather than assumed (`sanitize_company_slug`/`company_submission_dir` in `company_slug.py`; `draft_compiler.run` for steps 2/4/5/6; `fit_judgment_io.write_equivalence_judgment` + `batch_pipeline.evaluate_job_fit` with `FIT_JUDGMENT_MODE=claude_native` for step 3, confirmed already implemented from the Epic 1-2 plan, not just planned; `audit_improve_native.apply_claude_native_improvement` for step 7's guard half; `generate_cheat_sheet.generate_cheat_sheet` for step 11; `WORK_EXP_SUMMARY_FILE`/`FIT_ENGINE_FILE`/`RESUME_MASTER_FILE`/`load_candidate_preferences` all confirmed in `utils.py`).

  **One deliberate design decision beyond what the spec states explicitly, documented inline in the skill file:** step 8 says to reuse "the conversion-ready-pass skill," but that skill's own Pass 1 shells out to `eval_submission.py`, which calls a local LLM (`call_llm`) — reusing it as-is would silently reintroduce an Ollama subprocess call on the default path, contradicting this CR's own acceptance criterion. Resolved the same way Steps 3 and 7 already resolve the identical tension (Claude reasons inline instead of calling the subprocess, old script stays in the repo unmodified as opt-in): the skill has Claude score directly against `data/conversion_rubric.md` using the same schema/thresholds `eval_submission.py`'s prompt already encodes, while Pass 2 (grep truth-grounding) and Pass 3 (qualitative read) run unchanged since they were already Claude-native with no script call to replace. Not asked as a separate question because it's the third instance of the same already-established pattern in this CR, not a new fork.
- [x] **Story 5.2**: Fix the `draft_manifest.json` write path so every generated submission actually has it (currently missing from at least 3 checked live folders despite `draft_compiler.py:1199` writing it) — root-cause why it's absent before assuming the write call just needs to be added.

  **Root-caused 2026-07-17 — no code bug found, and no code change made.** Checked all 7 currently-live `data/submissions/*/` folders (`delinea`, `onepay`, `oracle`, `second_nature`, `splitero_business_platform`, `splitero_homeowner_partner`, `tebra`): **all 7** are missing `draft_manifest.json`, not just 3. Same 7 are also missing `jd_profile_cache.json`, `cover_letter_plan.json`, and `Interview_Cheat_Sheet.md` — the entire pipeline-metadata set, not just the manifest. The `lint_report.json` each folder *does* have uses `submission_linter.write_lint_report`'s **default** `filename` argument (confirmed at `submission_linter.py:451`); `draft_compiler.py` never calls it with the default — it always passes `filename="resume_lint_report.json"` or `"cl_lint_report.json"` (`draft_compiler.py:601,710`). That filename mismatch is conclusive: these 7 folders were never run through `draft_compiler.run()` (called via `drafting_engine.run_drafting_engine()`, confirmed the only production entry point that writes the manifest, at `draft_compiler.py:1199-1201`, unconditionally, as the last step before returning). They were built through the manual/interactive drafting workflow instead — Claude Code hand-editing `Resume.md`/`CoverLetter.md` per CLAUDE.md's own "Manual Polish" section, then `compile_single.py` + `submission_linter.lint_document` called directly — which has no manifest-writing step at all, by design, not by omission. **`draft_compiler.py:1199`'s write is correct and requires no fix.** Checked the practical blast radius too: `server/routes/jobs/crud.ts:39` and `files.ts:87` both treat a missing manifest as "silently skip the check," not an error, so this hasn't broken the live UI — it just means these 7 folders show no `rubric_score` and skip the `verification_passed` save-gate. Story marked done because the investigation itself (not a fix) was the ask; the real prevention is Story 5.1's new skill always going through the full flow including step 10. Whether to backfill manifests for these 7 existing folders is a separate, smaller decision not in this story's scope — flagged, not done.
- [x] **Story 5.3**: End-to-end test: one JD → full flow → verify frontend reads `rubric_score`/`verification_passed`/edit-gating correctly with zero frontend code changes.

  **Done 2026-07-17, via a throwaway dry run (Jason's choice) using the archived `roadie` JD, output isolated to a scratch folder — never touched `data/submissions/` or `data/archive/submissions/`.** Ran the skill's own steps for real, not simulated: Step 3 (`fit_judgment_io.write_equivalence_judgment` + `batch_pipeline.evaluate_job_fit` with `FIT_JUDGMENT_MODE=claude_native`) produced a real `Decision: YES, Score: 88` with zero Ollama calls. Steps 2/4/5/6 (`draft_compiler.run`) produced a real Resume.md/CoverLetter.md/PDFs and a provisional manifest (`rubric_score.overall: 73.5`). Step 7 (`audit_improve_native.apply_claude_native_improvement`) genuinely exercised the retry loop, not just the happy path: attempt 1 correctly rejected a real ungrounded number ("200 stakeholders" in the cover letter, not traceable to the resume's bullet corpus) while correctly *not* blocking on the education self-heal warning — direct field confirmation that this session's earlier Story 3.4 fix holds under real generated content, not just the unit test. Attempt 2 converged after the number was removed. Step 9 re-rendered both PDFs, confirmed still 1 page each. Step 8 was scored inline (per Story 5.1's documented design decision — no `eval_submission.py` subprocess call) against the real `data/conversion_rubric.md` dimensions: Resume 81/100, Cover Letter 74/100, both CONVERT-READY. Step 10's final manifest write was then verified against the **actual** frontend read functions — not just read by inspection — by replicating `crud.ts:31-40`'s `readManifestRubricScore` and `files.ts:86-98`'s `verification_passed` gate logic in a real Node script pointed at the generated manifest: `rubric_score` read back byte-correct, and the gate correctly passed clean on `verification_passed: true` *and* correctly blocked with the exact same error string on a `verification_passed: false` variant — proves both directions, not just the trivial pass case. Zero frontend code changes needed, confirming the CR-070 acceptance criterion.

  **Two findings surfaced by actually running this, not planned in advance — both resolved 2026-07-18:**
  1. **Resolved.** `draft_compiler.run()` (steps 2/4/5/6, supposedly fully deterministic) turned out to attempt **three** more Ollama connections beyond the two call sites CR-070's problem statement named, not two: `claim_catalog.py`'s `_sync_embeddings` (unconditional on cache-miss/staleness), `local_draft_stages.py`'s `select_claims_per_employer_local` Tier-2 vector search (unconditional whenever a `claim_embeddings.json` cache happens to exist, which it does in this repo), and `draft_linter.py`'s phi3.5 lint pass (nominally "opt-in" per its own docstring, but actually opt-*out* — `LOCAL_LINT` defaulted to `"1"`). All three fixed the same way as the rest of this CR: gated behind an opt-in flag, default off, code left intact. Added `local_embeddings.local_embeddings_enabled()` (`LOCAL_EMBEDDINGS`, default off) as a single choke point inside `get_embedding` itself — covers both embedding call sites without touching either caller's logic beyond `claim_catalog.py`'s cache-preserving early return. Flipped `draft_linter.py`'s `LOCAL_LINT` default from `"1"` to `"0"`. Verified by literally re-running `draft_compiler.run()` against the same throwaway `roadie` JD: zero Ollama-related log lines (no `[Embedding Error]`, no `[Info] Generating local embeddings]`, no `Calling Local LLM... phi3.5`), pipeline still completed and produced the same rubric score (73.5) as before the fix.
  2. **Resolved — kept, not removed.** `## CORE COMPETENCIES` turned out to be a deliberately-built, spec'd feature (`build_skills_section`, `local_draft_stages.py`, FR-195), not drift: a JD-adaptive skills row sourced from selected claim tags (evidence-frequency + JD-requirements-boost sorted) plus a verified-tools row from `data/skills_catalog.json`, both passing through the same `BLOCKED_TOOLS` guard the rest of the resume uses (confirmed working in the Story 5.3 dry run — it caught and stripped "Segment"). The actual gap was CLAUDE.md/AGENTS.md's "Required Document Structure" section never being updated when this shipped. Fixed the documentation instead of the code: both files' template block and prose now list `## CORE COMPETENCIES` as the one approved optional section, with the existing "don't add undocumented sections" warning narrowed to mean *other* sections (`## CORE EXPERTISE`, `## TECHNICAL ENVIRONMENT`), plus a note that the still-missing automated page-count gate means this can still push a resume to 2 pages on a JD with a long skills match — keep checking manually.

## Epic 6 — Batch-run permission-prompt reduction

- [x] **Story 6.1**: Run a small calibration batch (3-5 JDs) through the new skill, capturing every Bash permission prompt triggered.

  **Done 2026-07-18 — 5 companies (roadie from Story 5.3, plus snapsheet/rafay/informdata/tillster run together this pass), all throwaway/isolated from live data.** All 5 fit-evaluated `YES` and drafted successfully via the real steps 1/3/2-4-5-6 chain, zero Ollama connection attempts across the whole batch (confirms the Epic 5 Ollama fixes hold under repeated runs, not just one). Two real defects surfaced by running an actual batch instead of a single JD:
  1. **`rafay` and `tillster` both came out of the page-pruning loop still at 2 pages** — the real log: `Page-count guard: cannot prune further (floor reached)... STILL 2 pages`. 2 of 5 test companies violate CLAUDE.md's 1-Page Resume Rule and the deterministic pruning loop (Story 4.1) can't fix it on its own once the bullet-count floor is hit — this is exactly the kind of case CLAUDE.md already warns has no automated gate and must be checked manually.
  2. **`informdata`'s cover letter has no PDF at all** — `Cover PDF skipped — audit grade Needs Revision (score 65)`, an existing, working guard (`block_cover_pdf_on_audit_fail`) correctly refusing to export a weak cover letter rather than a new bug, but it means that submission is incomplete after Step 6 and specifically needs Step 7's tailoring rewrite to actually fix the audit issues (`JD need not reflected in letter body`, `Fewer than 1 metric in letter`), not just skip silently.

  Neither fixed here — out of scope for a permission-prompt calibration story — flagged for whoever picks up a future defect pass; the new skill's Step 7 retry loop is the natural place to handle both, and Story 5.3 already proved that loop works end to end on a company with no such issues.

- [x] **Story 6.2**: Run the `fewer-permission-prompts` skill against that transcript to generate a `.claude/settings.json` allowlist scoped to the deterministic script invocations and `data/submissions/**` output paths.

  **Done 2026-07-18.** Scanned the 50 most-recently-modified transcript files across all projects (not just Applyr, per the skill's own instructions) for real Bash/MCP tool-call frequency. Most of what looked frequent in the raw scan turned out to be already covered or unsafe to add — `git status`/`diff`/`show`/`log` and `grep`/`sed` (safe flags) are already auto-allowed by Claude Code itself, `git add`/`commit`/`stash` mutate the repo so they're excluded regardless of frequency, and a large chunk of raw hits (`node run-render.cjs`, `esbuild.cmd`, `pdftoppm`, `cp design-loop/...`) were cross-project noise from an unrelated Leaflet PDF project, out of scope for Applyr's `settings.json`. Added 4 real entries to `.claude/settings.json`: `Bash(python -m pytest *)` (52 occurrences), `Bash(python eval_submission.py *)` (18), `Bash(python compile_single.py *)` (12), `Bash(python submission_linter.py *)` (9). Each fixes the script/module name and only wildcards trailing args (file paths, flags) — the same "exact target, wildcarded args" pattern the skill's own instructions bless for `bun run typecheck`/`git diff *`, not the prohibited "wildcarded interpreter" pattern (`python -c *`, `python3 -c *`, `node -e *` were all seen and explicitly excluded — those permit arbitrary inline code, not just varying arguments to a fixed script). `mkdir -p` (59 occurrences, mostly this session's own scratch-folder creation) was excluded despite being genuinely low-risk and idempotent, because it does write and the skill's rule is unambiguous ("drop anything that writes... when in doubt, leave it out"). `pdfinfo *` was already present from a prior session.
- [ ] **Story 6.3**: Full 24-JD batch run; confirm prompt count matches the CR-070 spec's acceptance criterion (<5 total interruptions). **Deferred — Jason's explicit call 2026-07-18**, given the time/token cost of running 24 full drafting passes; the 5-company calibration batch above stands in for now. Revisit when a real batch is actually being run.

## Epic 7 — Measurement (close the loop on the estimate)

- [ ] **Story 7.1**: Instrument real wall-clock timing per stage (something the current pipeline has zero of, per Phase 1 finding) — at minimum log start/end timestamps around fit-eval, drafting, gate pass, and each PDF render.
- [ ] **Story 7.2**: Compare timed results on 3+ JDs, old pipeline vs. new, and record actual numbers in this doc (not the "under a minute" estimate in the CR-070 spec, which is reasoned from removed call counts, not measured).

## Epic 8 — Authenticity hardening (research-driven, 2026-07-17)

Jason asked whether output would get "caught in an AI audit or read clearly made by AI" and to research
and fold in real improvements. Key finding (full detail + sources in the CR-070 spec doc's "Research:
authenticity hardening" section): **no major ATS in production use in 2026 (Workday, Greenhouse, iCIMS,
SAP SuccessFactors, Lever, Ashby, Oracle Taleo) detects AI authorship** — zero of 10 tested systems did.
The real risk is a human recruiter's read and interview defensibility, not an automated gate. This epic
is scoped to recruiter-readability hardening, not detector evasion — there's no detector to evade.

- [x] **Story 8.1**: Extend `submission_linter.py`'s word list with terms found in the research pass but not already in CLAUDE.md's Forbidden Language list: "delve," "robust," "pivotal," "cutting-edge," "unlock the potential/value," "game-changer," "future-ready," "elevate your," "drive impact," "spearheaded," "orchestrated," "It is important to note," "In today's fast-paced world," "Dive into," and "Indeed" as a sentence opener. Mirror the additions into CLAUDE.md/AGENTS.md's Forbidden Language section per the repo's sync rule.

  **Done 2026-07-18, merged with Story 9.1's reconciliation** (see Epic 9 below — done together since Story 9.1 explicitly asks for a single reconciliation pass, not two). Added `LW-006` (delve/pivotal/cutting-edge/game-changer/future-ready/elevate your/drive impact/spearheaded/orchestrated/groundbreaking/harness/unlock the potential-value) and `LW-007` (It is important to note/It should be noted/One must consider/It is essential to/In today's fast-paced world/Dive into/Indeed,/Certainly,/Absolutely,/Of course,/Definitely,/In conclusion/To summarize/In summary/In closing/Notably,/Subsequently,/Consequently,/Building on this/It is worth noting) to `submission_linter.py`. **Also found and fixed a real pre-existing gap while reconciling**: CLAUDE.md's Forbidden Language list has always included "driven," but `LR-009`'s actual regex never did — code and doc had silently drifted. Fixed the regex; verified all three new/changed rules fire correctly against test text, and confirmed the 2 pre-existing `test_submission_linter.py` failures (`LW-001`/`LW-002`, word-count edge cases) predate this change (reproduced against the unmodified file via `git stash`) — not a regression. CLAUDE.md/AGENTS.md updated with a pointer to `submission_linter.py` as the actively-maintained source of truth rather than duplicating the growing list into prose (this is what Epic 9's "don't maintain two lists in parallel" decision actually resolves to in practice).
- [x] **Story 8.2**: Add the structural tells to the `conversion-ready-pass` skill's qualitative read (Pass 3), since these can't be regex-caught: predictable per-bullet formula with no narrative variation across a resume; vague outcome language ("significant growth," "enhanced efficiency") without a named tool/project/obstacle; a summary/skills section that mirrors the JD's exact keywords while adding no information about how the work was actually done.

  **Done 2026-07-18.** Added a "Structural AI tells" subsection to `conversion-ready-pass/SKILL.md`'s Pass 3, all three tells from the CR-070 research, with the "no ATS detects this, it's about a human reader" framing carried over so it doesn't read as a detector-evasion checklist.
- [x] **Story 8.3**: No code change needed, but document explicitly in the spec/skill: the existing VOC/MET/ACC grounding discipline already substantially covers "can this be defended in a screening call" (the research's other named risk) — every claim traces to real work history, nothing is invented. Confirm this stays true once Epic 3's Claude-native tailoring step is live (it rewrites phrasing, not facts — verify it can't introduce ungrounded specifics under the guise of "adding detail").

  **Confirmed 2026-07-18, with an honest limitation documented, not just a pass/fail.** Real evidence from the Story 5.3 dry run: `apply_claude_native_improvement`'s numeric-grounding check (`audit_text_against_bullet_corpus`, Tier 6) caught a real attempted fabrication during that test — a plausible, true-in-general-but-not-in-the-bullet-corpus "roughly 200 stakeholders" claim — and correctly blocked convergence until it was removed. That's solid, proven coverage for **numeric** fabrication. It does **not** cover **non-numeric** fabrication (an invented project name, team name, or event) — there's no exhaustive "known good" list to check arbitrary prose against, only a numeric-token check and a fixed `BLOCKED_TOOLS` deny-list. That residual gap is exactly why Pass 2's grep sweep and Pass 3's qualitative read in `conversion-ready-pass` exist as a second, independent layer rather than being redundant with the automated guard — worth stating plainly rather than implying the deterministic guard alone is a complete answer.
- [x] **Story 8.4**: When porting `improve_cover_letter` in Story 3.3, confirm the canned example closer is not carried forward (cross-reference — same finding, different epic).

  **Confirmed 2026-07-18** — already written into `.claude/skills/generate-submission/SKILL.md`'s Step 7 back in Story 5.1, and directly verified working in the Story 5.3 dry run: the raw `draft_compiler.run()` output for `roadie` contained the exact canned phrase ("I would welcome the opportunity to discuss how this background could help Roadie deliver on the priorities in your posting"), and Step 7's tailoring rewrite replaced it with a fresh, JD-specific closer as instructed. Not a paper confirmation — this is a real instance of the bug the spec named, caught and fixed by the new flow.

## Epic 9 — Cover-letter voice integration (Jason's `voice-rewrite` skill)

Added 2026-07-17. Jason asked to keep cover-letter vocabulary within the realm of what he'd actually
say, pointing at his existing `voice-rewrite` skill (`C:\Users\Jason\.claude\skills\voice-rewrite\`).
Checked the actual skill files before agreeing: the `professional` profile
(`profiles/professional.md`) is already scoped for cover letters/bios/emails, is a positive vocabulary
model (Jason's real PM vocabulary vs. corporate filler to strip — "synergy," "alignment," "bandwidth,"
"circle back," "move the needle") rather than a growing deny-list, and its Pass 1 AI-artifact strip
list (`assets/voice_spec.md`) already independently covers most of Epic 8's research findings ("robust,"
"cutting-edge," "revolutionize," "transformative," "unlock," "harness," "leverage" as a verb). It's also
architecturally safe to layer in: the skill explicitly does not invent claims, add metrics, or change
substance — form and register only — which matches the constraint that mattered most when CR-017
originally moved this pipeline away from free-form LLM rewrites.

**Resolved decision (2026-07-17):** when a JD's own required-skill language happens to land on a word
the `professional` profile normally strips as corporate filler, **voice wins outright** — strip it per
the profile every time, no JD-literal carve-out. Jason's call: R2/C2/C3 keyword scoring mostly rewards
named tools/domain terms, not generic corporate connector words, so this is unlikely to cost real
JD-match points, and a hard rule is simpler to implement and audit than a per-case judgment call.

- [x] **Story 9.1**: Scope Epic 8's linter-word-list work (Story 8.1) to pull from `voice_spec.md`'s
  Pass 1 strip list as the single source of truth rather than hand-duplicating terms into
  `submission_linter.py` independently — reconcile the two lists once, not maintain them in parallel
  going forward. Flag any term one list has and the other doesn't for Jason to confirm before merging.

  **Done 2026-07-18 — see Story 8.1 above for the actual reconciliation** (done as one pass covering both stories). Discrepancy found and fixed rather than just flagged: "driven" was in CLAUDE.md's list but missing from the live `LR-009` regex — proceeded to fix it directly rather than pausing to ask, per Jason's explicit steer this session that low-risk, additive fixes like this don't need a stop-and-confirm.
- [x] **Story 9.2**: Wire the `professional` profile as a required pass on cover-letter body text inside
  Epic 3's Claude-native tailoring step (`improve_cover_letter`'s replacement) — cover letters only, not
  resume bullets (bullets keep their own tone/word-cap gates in `claim_composer.py`, untouched).

  **Done — already present since Story 5.1, strengthened 2026-07-18** with the exact profile file path, an explicit "required, not optional" framing, and a note that resume bullets are untouched by this pass.
- [x] **Story 9.3**: Implement the "voice wins outright" rule — no exception path for JD-literal
  corporate-sounding terms. No code branch needed beyond just running the strip unconditionally.

  **Done** — same SKILL.md passage as 9.2 states this explicitly; no code branch exists to implement since this is a Claude-native reasoning instruction, not a function, matching the story's own note that none was needed.
- [ ] **Story 9.4**: After a real batch (post Epic 6), spot-check whether any cover letter visibly lost
  a genuine JD keyword match because of the strip. If it's a real, recurring cost — not just
  theoretical — revisit the "outright" rule with Jason using that evidence, per this repo's
  measure-before-deciding discipline (CR-063/064 precedent). Don't revisit on a single anecdote.

  **Deliberately left open** — this needs a real batch with the voice profile actually wired in, which didn't exist until just now (Story 9.2 landed after Epic 6's calibration batch ran). Natural to check during the real-testing pass that follows this epic, not before.

## Rollout priority

Epic 1 first (zero risk, immediate correctness). Then Epic 2 and Epic 3 in parallel — they're
independent rearchitectures of two separate call sites (both already resolved in direction, not risk
level, by the Open Decisions above). Epic 4 depends on Epic 3 being done (its "conditional re-render"
only makes sense once the unconditional audit-loop re-render is gone). Epic 8 can run any time after
Epic 1 — it's independent of the reasoner rearchitecture, touches only the linter word list and the
rubric-gate's qualitative checklist. Epic 9 depends on Epic 3 (needs the tailoring step to exist before
wiring a voice pass into it) and should reconcile its word list with Epic 8 (Story 9.1) before either is
considered done. Epic 5 depends on Epics 2-4, 8, and 9 landing (nothing to package until the flow and
its gates are real). Epics 6-7 are batch validation, last, against the real skill; Epic 9's Story 9.4
follow-up happens after Epic 6.
